language: objective-c
osx_image: xcode9.4

env:
  - ACTION=test  PLATFORM=Mac     DESTINATION='platform=OS X'
  - ACTION=test  PLATFORM=iOS     DESTINATION='platform=iOS Simulator,name=iPhone X'
  - ACTION=test  PLATFORM=tvOS    DESTINATION='platform=tvOS Simulator,name=Apple TV 4K (at 1080p)'

before_install:
  - bundle install
  - brew update
  - brew outdated xctool || brew upgrade xctool

script:
  - set -o pipefail && xcodebuild -scheme UserDefaultsStore -enableCodeCoverage YES -destination "$DESTINATION" $ACTION
after_success:
  - bash <(curl -s https://codecov.io/bash)
